[package]
name = "taskotter-gateway"
version = "0.1.0"
edition = "2021"
license = "PolyForm-Strict-1.0.0"
description = "TaskOtter AI Gateway and MCP runtime contract scaffold."
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
