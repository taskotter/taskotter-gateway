[package]
name = "taskotter-gateway"
version = "0.1.0"
edition = "2024"
license = "LicenseRef-PolyForm-Strict-1.0.0"
description = "MVP foundation for the TaskOtter MCP and AI gateway."
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
pretty_assertions = "1.4"
